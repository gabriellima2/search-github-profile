import Colors from '../Colors';

const Dark = {
    background: Colors.main.dark,
    complementary: Colors.complementary.dark,
    accent: Colors.accent.dark,
    neutral: Colors.neutral.dark
};

export default Dark;
